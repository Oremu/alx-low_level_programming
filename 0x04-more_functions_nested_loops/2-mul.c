#include <stdio.h>
#include "main.h"

/**
 * mul - entry point
 * @a: character to check
 * @b: another character to check
 * Description: multiplies two integers
 * Return: 0
 */
int mul(int a, int b)
{
	return (a * b);
}
